

<ul>

<% @projects.each do |project| %>
<h1><%= project.title %></h1>
<%= link_to image_tag(project.pict_path, :size => "80x80"), project_path(project.id) %>

<% end %>
アイテムが追加されました。
</ul>
<p>
</p>
<hr>
<h2>関連する商品</h2>

<% @item_code.each do |p| %>
<%= link_to image_tag(p.pict_path), project_path(p.id) %>

<% end %>

<hr>
<h2>カートの小計</h2>
